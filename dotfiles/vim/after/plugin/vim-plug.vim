""""""""""""""""
"              "
"   Vim-Plug   "
"              "
""""""""""""""""

" This is the only plugin not managed by a plugin manager (stored directly in
" the dotfiles repository) and as such it needs to be updated manually.  Use
" :PlugUpgrade to manually update vim-plug.  Run :PlugUpdate to update all
" plugins, or :PlugInstall to install missing items.

" open vim-plug in window rather than split
let g:plug_window = 'topleft'
